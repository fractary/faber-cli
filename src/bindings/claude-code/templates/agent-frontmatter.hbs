---
org: {{metadata.org}}
system: {{metadata.system}}
name: {{metadata.name}}
description: |
  {{metadata.description}}
{{#if metadata.platforms}}

  Supports platforms: {{#each metadata.platforms}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}
{{/if}}

{{#if metadata.color}}
color: {{metadata.color}}
{{/if}}
{{#if metadata.tags}}
tags: [{{#each metadata.tags}}"{{this}}"{{#unless @last}}, {{/unless}}{{/each}}]
{{/if}}
---